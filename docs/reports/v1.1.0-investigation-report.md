# ExtendAI Lab v1.1.0 调研报告

**日期**: 2026-05-20  
**目标**: 全面调研 OpenCode 官方文档，对比现有实现，制定 v1.1.0 统一修复方案

---

## 一、OpenCode 官方文档核心要点

### 1. **工具系统 (Tools)**

**官方机制**：
- 工具通过 `permission` 配置控制（`allow` / `ask` / `deny`）
- 支持通配符模式匹配（如 `mymcp_*`）
- 自定义工具必须放在 `.opencode/tools/` 或 `~/.config/opencode/tools/`
- 工具定义使用 `tool()` 辅助函数，提供类型安全
- **文件名即工具名**，单文件多工具导出格式为 `<filename>_<exportname>`

**关键发现**：
- 自定义工具可以**覆盖内置工具**（如果同名）
- 工具接收 `context` 参数：`{ agent, sessionID, messageID, directory, worktree }`
- 工具可以用任何语言编写（TypeScript/JavaScript 只是定义层）

### 2. **代理系统 (Agents)**

**官方机制**：
- **主代理 (Primary)**：用户直接交互，Tab 键切换
- **子代理 (Subagent)**：主代理调用或 `@` 提及
- 代理配置支持 JSON 和 Markdown 两种方式
- 代理可以覆盖全局权限、工具、模型

**关键发现**：
- `mode` 选项：`primary` / `subagent` / `all`
- `hidden: true` 可以隐藏子代理（不在 `@` 菜单显示）
- `permission.task` 控制代理可以调用哪些子代理（支持 glob 模式）
- **用户始终可以通过 `@` 直接调用任何子代理**，即使权限拒绝

### 3. **技能系统 (Skills)**

**官方机制**：
- 技能通过 `SKILL.md` 定义，必须包含 YAML frontmatter
- 必填字段：`name`、`description`
- 技能名称规则：1-64 字符，小写字母数字，单连字符分隔
- 技能通过 `skill` 工具按需加载

**关键发现**：
- 技能搜索路径：`.opencode/skills/`、`~/.config/opencode/skills/`、`.claude/skills/`、`.agents/skills/`
- 技能权限通过 `permission.skill` 控制（支持模式匹配）
- `tools.skill: false` 可以完全禁用技能功能

### 4. **命令系统 (Commands)**

**官方机制**：
- 命令通过 `.opencode/commands/` 中的 Markdown 文件定义
- 支持参数：`$ARGUMENTS`、`$1`、`$2` 等
- 支持 Shell 输出注入：`!`command``
- 支持文件引用：`@filename`

**关键发现**：
- 命令可以指定 `agent`、`model`、`subtask` 选项
- `subtask: true` 强制命令作为子代理运行
- 自定义命令可以**覆盖内置命令**

### 5. **权限系统 (Permissions)**

**官方机制**：
- 权限解析为 `allow` / `ask` / `deny`
- 支持对象语法进行细粒度控制（如 `bash: { "*": "ask", "git *": "allow" }`）
- **最后匹配的规则优先**
- 支持主目录展开（`~` 或 `$HOME`）

**关键发现**：
- `external_directory` 权限控制工作目录外的路径访问
- `doom_loop` 权限：同一工具调用重复 3 次时触发
- 代理权限会与全局配置合并，代理规则优先

### 6. **规则系统 (Rules)**

**官方机制**：
- 规则通过 `AGENTS.md` 文件定义
- 支持项目级（`.opencode/AGENTS.md`）和全局级（`~/.config/opencode/AGENTS.md`）
- 支持 Claude Code 兼容（`CLAUDE.md`、`~/.claude/CLAUDE.md`）

**关键发现**：
- 优先级：本地 `AGENTS.md` > 全局 `AGENTS.md` > `~/.claude/CLAUDE.md`
- 可以通过 `instructions` 字段引用外部文件（支持 glob 模式）
- 远程 URL 获取超时 5 秒

---

## 二、现有实现问题分析

### 问题 1：Auto-Review 机制冲突 ⚠️ **高优先级**

**现状**：
- `src/hooks/todo-continuation/index.ts` 第 32-66 行的 `REVIEW_PROMPT`
- 要求主 agent 调用 `task(subagent_type="oracle")` 启动子 agent 会话

**问题**：
- 违背"主 agent 优先"原则
- 启动子 agent 会话会创建新上下文，增加 token 成本
- 缓存失效，降低性能

**根因**：
- 设计时混淆了"子 agent 作为工具"和"子 agent 作为会话"
- @oracle 应该作为**内部 checklist**，而不是独立会话

**官方文档对照**：
- 官方文档中，子 agent 通过 `task` 工具调用是合法的
- 但我们的架构目标是"主 agent 优先"，应该避免频繁启动子会话

### 问题 2：Plan Mode 路径混乱 ⚠️ **高优先级**

**现状**：
- AI 知道要创建计划，但不知道存放路径
- 反复注入路径提示导致上下文混乱
- 有时输出到对话框而不是文件

**根因**：
- `save_plan` 工具的提示词不够明确
- 缺少路径验证和自动创建目录逻辑
- Plan agent 的权限配置可能有问题

**官方文档对照**：
- 官方文档没有 `save_plan` 工具（这是我们自定义的）
- 需要确保自定义工具的 `description` 足够清晰

### 问题 3：Start Work 命令检测失效 ⚠️ **高优先级**

**现状**：
- `/ol-start-work {plan-name}` 命令无法触发执行器
- 命令检测机制失效

**根因**：
- 可能缺少命令处理器注册
- 命令到 prompt 的转换逻辑有问题

**官方文档对照**：
- 官方命令系统通过 `.opencode/commands/` 中的 Markdown 文件定义
- 命令会转换为 prompt 注入到会话中
- 我们需要检查 `src/hooks/command-router/` 是否正确实现

### 问题 4：MCP 多窗口共享问题 🔧 **中优先级**

**现状**：
- stdio 传输 1:1 限制
- 第一个窗口正常，后续窗口失败

**根因**：
- MCP stdio 协议本身的限制
- 共享逻辑尝试复用进程但失败

**官方文档对照**：
- 官方文档没有提到多窗口共享
- 建议方案：移除共享逻辑，每个窗口独立启动（或使用随机端口）

### 问题 5：Skill 读取问题 🔧 **中优先级**

**现状**：
- 技能有时读取失败

**根因**：
- 可能是路径搜索逻辑有问题
- 或者 frontmatter 解析错误

**官方文档对照**：
- 技能搜索路径：`.opencode/skills/`、`~/.config/opencode/skills/`、`.claude/skills/`、`.agents/skills/`
- 必须包含 `name` 和 `description` 字段
- 技能名称必须符合正则：`^[a-z0-9]+(-[a-z0-9]+)*$`

### 问题 6：Delete Guard 未触发 🔧 **中优先级**

**现状**：
- 删除保护已注册但可能未触发

**根因**：
- 工具名称不匹配
- OpenCode 可能使用不同的工具名称

**官方文档对照**：
- 官方权限系统支持 bash 命令的细粒度控制
- 可以用 `bash: { "rm *": "deny" }` 来阻止删除命令

---

## 三、关键发现和建议

### 1. **插件注册机制**

**官方要求**：
- 工具、代理、命令、技能都应该通过 SDK 注册
- **不能**依赖用户手动修改 `opencode.json`
- 插件卸载后不应留下残留配置

**我们的实现**：
- ✅ 工具通过 MCP 服务器注册（正确）
- ✅ 代理通过插件 hooks 注册（正确）
- ❌ 技能和命令可能需要检查注册逻辑

### 2. **MCP 端口策略**

**建议方案**：
- **方案 A**：移除共享逻辑，每个窗口独立启动（简单）
- **方案 B**：使用随机端口，避免冲突（复杂）
- **方案 C**：等待 OpenCode 官方 SSE 支持（长期）

**推荐**：方案 A，简单可靠

### 3. **Auto-Review 重构**

**建议方案**：
- 将 @oracle 的 review 逻辑改为主 agent 内部 checklist
- 移除 `task(subagent_type="oracle")` 调用
- @oracle 仍保留为用户手动调用的专家 agent

### 4. **命令系统完善**

**需要实现**：
- `/ol-start-work` 命令处理器
- 命令到 prompt 的转换逻辑
- 命令参数解析和验证

---

## 四、v1.1.0 修复优先级

### P0 - 核心修复（必须完成）

1. **Auto-Review 重构**
   - 移除子 agent 调用
   - 改为主 agent 内部 checklist
   - 保留 @oracle 作为手动调用的专家 agent

2. **Start Work 命令检测修复**
   - 实现 `/ol-start-work` 命令处理器
   - 添加命令到 prompt 的转换逻辑

3. **MCP 多窗口问题修复**
   - 移除共享逻辑
   - 每个窗口独立启动 MCP 服务器

### P1 - 优化项（建议完成）

4. **Plan Mode 路径优化**
   - 优化 `save_plan` 工具的 description
   - 添加路径验证和自动创建目录

5. **Delete Guard 完善**
   - 扩展工具名称匹配
   - 添加 bash 命令细粒度控制

6. **Skill 读取优化**
   - 检查路径搜索逻辑
   - 优化 frontmatter 解析

### P2 - 文档更新

7. **README 更新**
   - 更新架构说明
   - 添加 v1.1.0 CHANGELOG

8. **AGENTS.md 更新**
   - 更新开发指南
   - 添加新的最佳实践

---

## 五、下一步行动

1. **立即行动**：
   - 检查现有代码实现
   - 对比官方文档找出所有差异
   - 制定详细的修复计划

2. **需要你确认**：
   - @oracle 是否保留为手动调用的专家 agent？
   - MCP 共享逻辑是否同意移除？
   - 版本号确认为 v1.1.0？

3. **实施顺序**：
   - P0 核心修复 → P1 优化项 → P2 文档更新
   - 每个修复完成后运行测试验证
   - 最后统一提交并发布 v1.1.0

---

**报告完成时间**: 2026-05-20  
**下一步**: 等待你的确认后开始实施修复方案
