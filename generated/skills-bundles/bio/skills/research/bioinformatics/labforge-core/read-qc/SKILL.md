---
name: "read-qc"
description: "FASTQ quality control and preprocessing guidance."
metadata:
  category: "research/bioinformatics/labforge-core"
  discovery_hidden: "true"
  catalog_tier: "leaf"
  upstream_source: "OpenAgent Labforge"
  routes_to_builtin: "read-qc"
---
# Labforge Wrapper: read-qc

FASTQ quality control and preprocessing guidance.

This is a category-routed wrapper for an existing OpenAgent Labforge built-in bio skill.

After reviewing this wrapper, immediately invoke `skill(name="read-qc")` to load the full built-in guidance.

Use this wrapper only when you entered through the bioinformatics category directory and need the exact built-in target.