# Plugins

This directory is reserved for companion plugins that ship alongside the core `openagent-labforge` harness.

Planned:

- `plugins/opencode-agent-bio-paper`
- `plugins/opencode-mcp-paper-search`
